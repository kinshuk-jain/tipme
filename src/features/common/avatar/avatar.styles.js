import { StyleSheet } from 'react-native';
import { COLORS } from '../../config';

export const styles = StyleSheet.create({
  fillContainer: {
    flex: 1,
    borderWidth: 1,
    borderColor: COLORS.GREY_MYSTIC,
  },
});
